interface:
  display_name: "Snapshot Test"
  short_description: "Build and verify snapshots."
  default_prompt: "Use this skill to validate changes by writing snapshot tests, generating images, inspecting visuals, and iterating fixes until tests and visuals both pass."
