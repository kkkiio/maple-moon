///|
fn install_debug_bindings(
  list_ui_entries_text : () -> String,
  click_ui : (String, String) -> String,
  render_game_to_text : () -> String,
  run_command_text : (String, Array[String]) -> String,
) -> Unit {
  ignore(list_ui_entries_text)
  ignore(click_ui)
  ignore(render_game_to_text)
  ignore(run_command_text)
}
