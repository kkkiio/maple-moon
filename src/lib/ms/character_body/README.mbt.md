# character_body

Character body data model and draw-info parsing.

- Defines body-related enums and frame data types (`BodyLayer`, `NxStanceBody`, ...).
- Parses stance/frame body resources into runtime draw info (`BodyDrawInfo`).
